
void drawTextAccessPrivateMemebr()
{
	GLfloat depth = -12.0f;
    glLoadIdentity();
    glTranslatef(-7.75f, 0.0f, depth);
    Draw_A();

    glLoadIdentity();
    glTranslatef(-6.8f, 0.0f, depth);
    Draw_C();

    glLoadIdentity();
    glTranslatef(-6.20f, 0.0f, depth);
    Draw_C();

    glLoadIdentity();
    glTranslatef(-5.9f, 0.0f, depth);
    Draw_E();

    glLoadIdentity();
    glTranslatef(-5.2f, 0.0f, depth);
    Draw_S();

    glLoadIdentity();
    glTranslatef(-4.5f, 0.0f, depth);
    Draw_S();

    glLoadIdentity();
    glTranslatef(-2.25f, 0.0f, depth);
    Draw_P();

    glLoadIdentity();
    glTranslatef(-1.5f, 0.0f, depth);
    Draw_R();

    glLoadIdentity();
    glTranslatef(-0.75f, 0.0f, depth);
    Draw_I();

    glLoadIdentity();
    glTranslatef(0.0f, 0.0f, depth);
    Draw_V();

    glLoadIdentity();
    glTranslatef(1.0f, 0.0f, depth);
    Draw_A();

    glLoadIdentity();
    glTranslatef(1.3f, 0.0f, depth);
    Draw_T();

    glLoadIdentity();
    glTranslatef(2.0f, 0.0f, depth);
    Draw_E();

    glLoadIdentity();
    glTranslatef(4.0f, 0.0f, depth);
    Draw_M();

    glLoadIdentity();
    glTranslatef(4.75f, 0.0f, depth);
    Draw_E();

    glLoadIdentity();
    glTranslatef(5.5f, 0.0f, depth);
    Draw_M();

    glLoadIdentity();
    glTranslatef(6.25f, 0.0f, depth);
    Draw_B();

    glLoadIdentity();
    glTranslatef(7.0f, 0.0f, depth);
    Draw_E();

    glLoadIdentity();
    glTranslatef(7.75f, 0.0f, depth);
    Draw_R();
}
